#include<bits/stdc++.h>
using namespace std;

int main(){
    // max heap
    priority_queue<int> max ;

    priority_queue<int,vector<int>,greater<int>min;
    maxi.push(1);
    maxi.push(2);
    maxi.push(4);
    maxi.push(0);
    int n=max.size();
    for(int i=0;i<=n;i++){
        cout<<maxi.top()<<" "; //4 2 1 0
        maxi.pop();
    }
    // cout<<i<<" ";

    for(int i :l)
}
